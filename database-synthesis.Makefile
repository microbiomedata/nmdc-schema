prompt.txt:
	{ echo "Here is the NMDC schema, which follows the LInkML meta language and is serialized in YAML.\n"; cat nmdc_schema/nmdc_schema_merged.yaml; echo "\nHere is an example valid data file what instantiates the Database class\n"; cat src/data/gemini/Database-studies-biosamples-etc.yaml; echo "\nPlease generate a large Database that uses as many of its slots as possible to collect several instances of as many classes as possible. Populate a variety of the slots for each instance, always providing valid values for all of the required slots. Maximize the interconnectedness of the instances with instances of different classes in other collections." ; } > $@